import { Injectable } from '@angular/core';
import { BehaviorSubject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class ItemIdentiferService {

  // private itemId = new BehaviorSubject('');
  // currentItemId = this.itemId.asObservable();

  // setItemId(id:string){
  //   this.itemId.next(id);
  // }
   
}
